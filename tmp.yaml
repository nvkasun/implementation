ince you're currently in CMD, run this exact command:

powershell -NoProfile -Command "Test-NetConnection 10.114.80.10 -Port 22 | Format-List ComputerName,RemoteAddress,RemotePort,SourceAddress,TcpTestSucceeded"

Send me that output, especially:

SourceAddress
TcpTestSucceeded

I expect TcpTestSucceeded : True, because SSH already says Connection established.

Now let's prove whether your connection actually reaches this VM

Your previous:

journalctl -u sshd --since "5 minutes ago"

returned:

-- No entries --

That's suspicious, but on RHEL we should also check /var/log/secure, because that's commonly where SSH authentication/session messages are recorded.

First attempt SSH once again from your PC:

ssh azureuser@10.114.80.10

After it fails, immediately run this through Azure VM → Run command → RunShellScript:

echo "=== SSH LOG ==="
grep -i sshd /var/log/secure | tail -50

echo
echo "=== LOCAL SSH TEST ==="
timeout 5 bash -c 'exec 3<>/dev/tcp/127.0.0.1/22; head -n 1 <&3'

For the second test I expect something similar to:

SSH-2.0-OpenSSH_8.x

or:

SSH-2.0-OpenSSH_9.x