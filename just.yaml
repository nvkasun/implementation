On VM1, run these exact commands:

sudo systemctl status oracle-health.service
curl -i http://127.0.0.1:8080/health
sudo firewall-cmd --list-all
sudo ss -lntp | grep :8080

We want to confirm four things:

oracle-health.service     active/running
/health                   HTTP 200 + HEALTHY
port 8080                 listening
168.63.129.16/32:8080     allowed

If this rule is missing from VM1:

rule family="ipv4" source address="168.63.129.16/32" port port="8080" protocol="tcp" accept

restore it with:

sudo firewall-cmd --permanent \
  --add-rich-rule='rule family="ipv4" source address="168.63.129.16/32" port port="8080" protocol="tcp" accept'

sudo firewall-cmd --reload

Then verify:

sudo firewall-cmd --list-all

VM1 should ultimately contain both:

168.63.129.16/32 → TCP 8080   # Azure LB health probe
10.114.32.135/32 → TCP 1521   # VM3 application traffic

Do the same --list-all check on VM2 so both machines are symmetrical.

Then wait around 15–30 seconds for several Azure health-probe cycles and rerun:

./send-data.py 20

What we want next is something like:

Backend Distribution
--------------------
vm-001-dev-uan-a3975m     8 connection(s)
vm-002-dev-uan-19yf0j    12 connection(s)

FINAL RESULT: PASS

Not necessarily exactly 10/10.

So bro, don't change the Python script now. The script is doing its job correctly. The next investigation is VM1's LB health eligibility. Start with:

sudo firewall-cmd --list-all

and

curl -i http://127.0.0.1:8080/health