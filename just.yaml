On VM1:

sudo systemctl stop firewalld

Confirm:

sudo systemctl status firewalld --no-pager

It should show:

inactive (dead)

Do not run systemctl disable firewalld. We only want a temporary diagnostic.

Then do these tests in this order:

From Windows jump VM, test the health endpoint again:
curl -v --noproxy "*" http://10.114.32.132:8080/health
Test Oracle directly on VM1:
powershell -Command "Test-NetConnection 10.114.32.132 -Port 1521"

We want:

TcpTestSucceeded : True
Then test the actual Load Balancer frontend VIP:
powershell -Command "Test-NetConnection 10.114.32.134 -Port 1521"