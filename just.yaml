The module already uses azurerm_virtual_machine without os_profile for the existing specialized-disk attach mode. The specialized Gallery image path appears to need a similar legacy-resource path, but with storage_image_reference.id = source_image_id and create_option = "FromImage" rather than Attach.

And bro, Microsoft explicitly says a specialized image keeps the existing machine/account information and does not have an osProfile, exactly matching the Azure error you hit.

https://learn.microsoft.com/en-us/azure/virtual-machines/vm-specialized-image-version?utm_source=chatgpt.com&tabs=cli%2Ccli2%2Ccli3%2Ccli4%2Ccli5