Subject: Support Required – Storage Account Creation Blocked by CMK Policy

Hi Team,

We are trying to provision an Azure Storage Account using the approved corporate azure-terraform/modules/storage module.

The module is configured with:

customer_managed_key  = true
managed_identity_type = "UserAssigned"

The Terraform plan confirms that the Storage Account includes both the Customer Managed Key configuration and the User Assigned Managed Identity. We are currently using AzureRM provider v4.81.0.

However, during terraform apply, Storage Account creation is rejected with the following policy error:

RequestDisallowedByPolicy:
Customer-Managed Keys (CMK) must be enforced at the time of resource creation.

We have retried the deployment after allowing time for the Key Vault/RBAC permissions to propagate, but the same Storage Account policy error remains.

Could you please advise whether there is an approved version/configuration of the corporate Storage module that supports the CMK-at-resource-creation policy, or if any additional module configuration is required?

I can share the Terraform plan and apply logs if required.

Thanks,
Kasun