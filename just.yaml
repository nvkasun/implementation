On VM1 run:

sudo ss -lntp | grep ':1521'

Then:

timeout 3 bash -c '</dev/tcp/127.0.0.1/1521' \
  && echo "LOCAL 1521 OPEN" \
  || echo "LOCAL 1521 FAILED"

And importantly:

timeout 3 bash -c '</dev/tcp/10.114.32.132/1521' \
  && echo "PRIVATE-IP 1521 OPEN" \
  || echo "PRIVATE-IP 1521 FAILED"