Hi Ambrose,

Thank you for the detailed clarification regarding the automatic backup window time change. In addition to that, we would like to follow up on the second concern we discussed yesterday regarding the planned Multi-AZ enablement for our existing production RDS for Oracle database.

We would like to clarify the following two points before proceeding:

During Multi-AZ enablement:
Currently, our existing Read Replica is asynchronously replicating from the primary RDS Oracle instance. While enabling Multi-AZ, if any failover or change of the active primary occurs during the enablement process, will the existing Read Replica automatically reconnect and continue replication from the active primary? Or would any manual intervention be required?
After Multi-AZ enablement:
Once Multi-AZ is enabled, if a future failover occurs and the standby instance in the secondary AZ becomes the new primary, will our existing Read Replica automatically switch its replication source to the newly promoted primary and continue replication without any manual intervention?

Could you please confirm the expected behavior for these two scenarios?