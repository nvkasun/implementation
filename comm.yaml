Azure VM → Run command → RunShellScript

Run:

timeout 30 tcpdump -nn -i any tcp port 22

Immediately after clicking Run, go to your local Command Prompt and run:

ssh azureuser@10.114.80.10

Let it fail with:

kex_exchange_identification: Connection closed by remote host