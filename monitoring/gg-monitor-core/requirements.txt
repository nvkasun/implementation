# Governance gate (fix 4, manager-alignment correction): this file is NOT
# pip-installed by the Dockerfile against public PyPI. It documents exactly
# what the approved private-ECR BASE_IMAGE (or a future approved internal
# package repository) must already provide -- see the MONITOR BASE IMAGE
# GOVERNANCE GATE comment in Dockerfile.
boto3==1.34.144
botocore==1.34.144
PyYAML==6.0.1
jmespath==1.0.1
python-dateutil==2.9.0.post0
s3transfer==0.10.2
six==1.16.0
urllib3==2.2.2
