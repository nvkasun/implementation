# Pinned runtime dependencies, installed during the Docker build (see
# Dockerfile) -- same pinned-requirements.txt pattern already proven by the
# previous successful monitor implementation (monitoring/monitor/requirements.txt).
# Every version here is exact (==), never a floor/range, so the image is
# reproducible. PyYAML is required by this module (topology/inventory
# parsing) but was not part of the previous monitor's own requirements.
boto3==1.34.144
botocore==1.34.144
PyYAML==6.0.1
jmespath==1.0.1
python-dateutil==2.9.0.post0
s3transfer==0.10.2
six==1.16.0
urllib3==2.2.2
