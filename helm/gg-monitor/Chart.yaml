apiVersion: v2
name: gg-monitor
description: Shared, passive GoldenGate runtime poller/writer (Phase 4) -- LEASE ownership, STATE#_deployment/STATE#<process> writes, manager-compatible CloudWatch metrics
type: application
version: 0.1.0
appVersion: "1.0.0"
