# BLOCKER (reported, not fabricated -- see the correction-pass completion report's "TERRAFORM STATE DESIGN" section): this root needs its own isolated remote state per runtime deployment ID, but the exact backend convention this repository uses is not locally provable -- envs/dev/main.tf's `backend "s3" {}` is an empty/partial block whose bucket/key/region/lock-table values are supplied entirely externally, through .github/workflows/gg-iam-secrets-deployment.yaml delegating to the external reusable workflow AbuDhabiCommercialBank/adcb-reusable-workflows/aws-terraform-apply.yaml@main (outside this repository, not inspectable here); no bucket name, lock table, account ID, or -backend-config value is invented below. `terraform init` for this root cannot be safely wired into the deployment workflow until the platform team confirms how that reusable workflow can be pointed at a distinct working directory/state key -- see the completion report for the exact follow-up question.
terraform {
  backend "s3" {}
}
