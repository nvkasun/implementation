Complete the last infrastructure checks
Verify the monitor ServiceAccount
kubectl get serviceaccount gg-monitor \
  -n goldengate-monitoring \
  -o jsonpath='{.metadata.annotations.eks\.amazonaws\.com/role-arn}{"\n"}'

Expected:

arn:aws:iam::668311715351:role/GoldenGateMonitorReadRole-dev
Verify the SecretProviderClass
kubectl get secretproviderclass \
  -n goldengate-monitoring
Verify mounted filenames only
kubectl exec \
  -n goldengate-monitoring \
  deployment/gg-monitor \
  -- find /mnt/secrets-store \
  -maxdepth 1 \
  -type f \
  -printf '%f\n'

Expected:

gg-oracle-payments-01-admin-user
gg-oracle-payments-01-admin-password
gg-postgresql-payments-01-admin-user
gg-postgresql-payments-01-admin-password
ca-chain-pem

Do not print the contents.

Check warnings and errors
kubectl logs \
  -n goldengate-monitoring \
  deployment/gg-monitor \
  --since=30m |
grep -Ei 'WARN|ERROR|AccessDenied|credential unavailable|TLS trust|CONFIG unavailable|Traceback'

No output is ideal.