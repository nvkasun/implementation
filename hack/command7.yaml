Hi Paleri,

I have completed the GoldenGate DNS configuration as well. You can access the Argo CD console using the below URL:

https://argocd.goldengate-dev.adcbmis.local

The CI/CD implementation is also completed. Whenever we make any changes to the main branch, the GitHub Actions workflow will automatically detect the changes, build the Helm OCI package, and push it to Amazon ECR. Argo CD will then continuously monitor the latest version and deploy the changes to the EKS cluster.

Below are the URLs for the current Oracle-to-PostgreSQL GoldenGate deployment:

Oracle GoldenGate Source Portal
https://ogg-oracle-payments-ora-to-pg-001.goldengate-dev.adcbmis.local

PostgreSQL GoldenGate Target Portal
https://ogg-postgresql-payments-ora-to-pg-001.goldengate-dev.adcbmis.local

The next development phase will focus on the GoldenGate runtime configuration, including:

Oracle source and PostgreSQL target database connectivity
GoldenGate database credential configuration
Extract and local trail creation
Distribution path configuration
Receiver and remote trail validation
Replicat configuration
End-to-end Oracle-to-PostgreSQL replication testing
Recovery, monitoring, and rollback validation