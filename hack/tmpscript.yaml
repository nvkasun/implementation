No more AWS Console screenshots are needed yet, bro.

Run just this:

kubectl get deployment efs-csi-controller \
  -n kube-system \
  -o jsonpath='{range .spec.template.spec.containers[?(@.name=="efs-plugin")].args[*]}{.}{"\n"}{end}'

Also run this if Helm is available:

helm get values aws-efs-csi-driver \
  -n kube-system \
  --all

For the first command I'm specifically looking for something like:

--tags=...

or any custom/corporate tags.