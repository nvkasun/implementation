Hi Team,

We have validated the internal ALB and Kubernetes Ingress routing for the GoldenGate EKS POC. Both source and target GoldenGate services are reachable through the internal ALB using host-based routing.

Current internal ALB DNS:
internal-k8s-ggpocdevalb-53c8cf7cb4-847733536.eu-west-1.elb.amazonaws.com

We checked the certificate currently attached to the ALB, and the Subject Alternative Name appears to include only:

DNS Name=goldengate-dev.adcbmis.local

For the GoldenGate deployment model, each deployment will expose separate source and target admin endpoints using host-based routing, for example:

ogg-oracle-payments-ora-to-pg-003.goldengate-dev.adcbmis.local
ogg-postgresql-payments-ora-to-pg-003.goldengate-dev.adcbmis.local

Could you please update or issue the ACM certificate to include the wildcard SAN below?

*.goldengate-dev.adcbmis.local

Once the wildcard certificate is available and attached to the existing internal ALB, we will update the Helm Ingress hostnames accordingly and request the private Route53 wildcard DNS record.

Kind regards,
Kasun